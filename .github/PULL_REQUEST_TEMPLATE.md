## What changed and why

## How was this verified?
- [ ] `npm run build` passes
- [ ] Tested in paper mode (`EXECUTION_MODE` unset or not `live`) before any live-mode test
- [ ] If this touches the discipline/risk-cap or swap execution path: tested against a real deployment with a real (small) swap, not just typechecked

## Anything a reviewer should look at closely?
